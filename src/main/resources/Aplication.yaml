spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/laba4-tochno_ready
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
    mvc:
      view:
        prefix: /resources/templates/
        suffix: .ftlh
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false

